<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions required for REST API communication and connectivity checking -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 
        Note on Cleartext Traffic:
        Cleartext HTTP traffic is enabled below specifically for local development to communicate with the C# Web API 
        hosted on the development machine (LAN IP) during physical phone testing.
        Production and deployed environments MUST use HTTPS and disable cleartext traffic.
    -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartSolarMicrogridTradingSystem"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".feature.dashboardmaps.ui.ReservationDetailActivity"
            android:exported="false" />
        <activity
            android:name=".feature.dashboardmaps.ui.ReservationSearchActivity"
            android:exported="false" />
        <activity
            android:name=".feature.dashboardmaps.ui.BookingListActivity"
            android:exported="false" />
        <activity
            android:name=".feature.dashboardmaps.ui.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
