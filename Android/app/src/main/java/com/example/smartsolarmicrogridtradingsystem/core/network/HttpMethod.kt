package com.example.smartsolarmicrogridtradingsystem.core.network

/**
 * Standard HTTP methods supported by the REST client.
 */
enum class HttpMethod {
    GET,
    POST,
    PUT,
    PATCH,
    DELETE
}
